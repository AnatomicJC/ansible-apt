---
apt_32_bit: False
apt_allow_unauthenticated: False
apt_eatmydata: False
apt_squid_proxy: False
apt_preferences: []
apt_clean_old_config_files: False
#  - dest: "/etc/apt/preferences.d/openssh"
#    content: |
#      Package: openssh-server openssh-client ssh
#      Pin: release a=wheezy-backports
#      Pin-Priority: 500

apt_proxy_url: ''
apt_cache_update: "yes"
apt_cache_valid_time: "3600"
apt_main_url: "deb.debian.org"
apt_keys: []
#  - hwraid.le-vert.net.gpg.key
#  - mysql.gpg.key
#  - nodesource.gpg.key
#  - mssql.gpg.key

apt_sources_list:
  - main
  - security
#  - hwraid
#  - backports
#  - proxmox
#  - mysql.5.7
#  - nodesource
#  - mssql
